# Message Broker

A small message broker written in Rust.

The current version provides a command-line REPL for adding, removing, and listing messages from an in-memory queue.

## Running

```bash
cargo run
```
